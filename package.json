{
  "name": "simple-typescript-game",
  "version": "1.0.0",
  "description": "A simple browser-based game using TypeScript",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run copy-files",
    "copy-files": "mkdir -p public/styles public/dist && cp src/index.html public/ && cp src/styles/style.css public/styles/ && cp -r dist/* public/dist/",
    "watch": "tsc -w",
    "start": "http-server -c-1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "three": "^0.157.0",
    "@types/three": "^0.157.0"
  }
}
